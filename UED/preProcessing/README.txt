Preprocessing takes files from original format and processes them and saves them in root files.
  background subtraction
  centering image
  remove edges with no data
  make polar binned histogram
  etc.


Data:
  ./preProcessing.exe runFullRev.txt

Simulation:
  ./preProcessingMC.exe runMClist.txt 
